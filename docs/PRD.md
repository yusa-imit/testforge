# PRD: 자동화 테스트 플랫폼 (TestForge)

> **Version**: 1.0.0  
> **Last Updated**: 2026-02-07  
> **Status**: MVP 정의

---

## 1. 개요

### 1.1 제품 비전

프론트엔드/백엔드를 아우르는 통합 자동화 테스트 플랫폼. QA 엔지니어와 기획자가 코드 작성 없이 체계적으로 테스트를 생성, 관리, 실행할 수 있으며, 서비스 변경 시 테스트가 스스로 적응(Self-Healing)하는 지능형 테스트 시스템.

### 1.2 핵심 가치

| 가치 | 설명 |
|------|------|
| **체계적 관리** | 서비스 → 기능 → 시나리오 계층 구조로 테스트 조직화 |
| **재사용성** | 공통 플로우를 컴포넌트로 추출하여 중복 제거 |
| **Self-Healing** | UI/API 변경 시 자동 감지 및 복구, 승인 워크플로우 |
| **낮은 진입장벽** | QA/기획자가 직접 테스트 작성 가능 (노코드/로우코드) |

### 1.3 타겟 사용자

| 사용자 | 역할 |
|--------|------|
| **QA 엔지니어** (Primary) | 테스트 시나리오 작성, 실행, 결과 분석, Self-Healing 승인 |
| **기획자** (Primary) | 기능 명세 기반 테스트 케이스 작성, 커버리지 확인 |
| **개발자** (Secondary) | 커스텀 액션 개발, 어댑터 확장, 테스트 결과 참조 |

### 1.4 범위

**MVP 포함:**
- 브라우저(E2E) 테스트 실행
- HTTP API 테스트 실행
- 계층적 테스트 관리 (서비스/기능/시나리오)
- 재사용 가능한 컴포넌트
- 다층 셀렉터 기반 Self-Healing
- Healing 승인 워크플로우
- 웹 기반 UI

**MVP 제외:**
- 클라우드 배포 / 멀티 테넌시
- CI/CD 통합
- 모바일 앱 테스트
- 성능/부하 테스트
- AI 자연어 테스트 생성 (Phase 2)

---

## 2. 기술 스택

### 2.1 런타임 및 패키지 매니저

```
Runtime: Bun (latest stable)
Package Manager: Bun
Monorepo: Bun workspace
```

### 2.2 프론트엔드

```
Framework: React 18+
Build: Rsbuild
Routing: React Router
State: Zustand
Styling: Tailwind CSS
UI Components: shadcn/ui
HTTP Client: Axios (RPC 클라이언트 래핑)
```

### 2.3 백엔드

```
Framework: Hono (Bun 네이티브)
Database: DuckDB (분석 친화적, 단일 파일)
ORM: Drizzle ORM
RPC: Hono RPC (타입 안전한 클라이언트-서버 통신)
Queue: 초기엔 인메모리, 추후 BullMQ
Validation: Zod
```

### 2.4 테스트 실행 엔진

```
Browser Automation: Playwright
HTTP Testing: Bun 내장 fetch + Zod 검증
```

### 2.5 기술 선택 이유

| 기술 | 선택 이유 |
|------|----------|
| **Bun** | 빠른 실행, 올인원 (런타임 + 패키지 매니저 + 번들러), TypeScript 네이티브 |
| **Rsbuild** | Rspack 기반 빠른 빌드, Vite 대비 프로덕션 최적화 우수, React 친화적 |
| **shadcn/ui** | 복사-붙여넣기 방식으로 완전한 커스터마이징 가능, Radix 기반 접근성 |
| **Hono RPC** | 동일 레포에서 타입 공유, 런타임 오버헤드 없음, End-to-end 타입 안전 |
| **DuckDB** | 분석 쿼리 최적화 (테스트 통계, 트렌드), 컬럼 기반, 서버리스 |
| **Axios** | 인터셉터, 요청 취소, 진행률 추적 등 풍부한 기능 |

### 2.6 프로젝트 구조

```
testforge/
├── bun.lockb
├── bunfig.toml
├── package.json              # workspace root
│
├── packages/
│   ├── core/                 # 핵심 로직 (테스트 실행, Self-Healing)
│   │   ├── src/
│   │   │   ├── executor/     # 테스트 실행 엔진
│   │   │   ├── healing/      # Self-Healing 로직
│   │   │   ├── locator/      # 다층 셀렉터 해석
│   │   │   └── types/        # 공통 타입 정의
│   │   └── package.json
│   │
│   ├── server/               # API 서버 (Hono)
│   │   ├── src/
│   │   │   ├── routes/       # API 라우트
│   │   │   ├── services/     # 비즈니스 로직
│   │   │   ├── db/           # Drizzle 스키마 & 마이그레이션
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   └── web/                  # 프론트엔드 (React + Rsbuild)
│       ├── src/
│       │   ├── components/   # shadcn/ui 컴포넌트
│       │   ├── pages/
│       │   ├── stores/
│       │   ├── lib/
│       │   │   └── rpc.ts    # RPC 클라이언트 (타입 공유)
│       │   └── main.tsx
│       ├── rsbuild.config.ts
│       └── package.json
│
├── scripts/                  # 개발 스크립트
│   ├── dev.ts               # 동시 실행 (server + web)
│   └── seed.ts              # DB 시드 데이터
│
└── docs/
    └── PRD.md
```

---

## 3. 데이터 모델

### 3.1 핵심 엔티티

#### Service (서비스)

테스트 대상 애플리케이션의 최상위 단위.

```typescript
interface Service {
  id: string;                    // UUID
  name: string;                  // "채용 서비스"
  description?: string;
  baseUrl: string;               // "http://localhost:3000"
  
  // 기본 설정
  defaultTimeout: number;        // ms, 기본 30000
  
  createdAt: Date;
  updatedAt: Date;
}
```

#### Feature (기능)

서비스 내 논리적 기능 단위.

```typescript
interface Feature {
  id: string;
  serviceId: string;
  
  name: string;                  // "지원자 관리"
  description?: string;
  
  // 메타데이터
  owners: string[];              // 담당자 이름/이메일
  
  createdAt: Date;
  updatedAt: Date;
}
```

#### Scenario (시나리오)

실제 테스트 케이스.

```typescript
interface Scenario {
  id: string;
  featureId: string;
  
  name: string;                  // "지원자 상태 변경 - 서류합격"
  description?: string;
  
  // 분류
  tags: string[];                // ["smoke", "regression"]
  priority: 'critical' | 'high' | 'medium' | 'low';
  
  // 테스트 구성
  variables: Variable[];         // 테스트에서 사용할 변수
  steps: Step[];                 // 실행 스텝
  
  // 버전 관리
  version: number;
  
  createdAt: Date;
  updatedAt: Date;
}

interface Variable {
  name: string;                  // "userEmail"
  type: 'string' | 'number' | 'boolean' | 'json';
  defaultValue?: any;
  description?: string;
}
```

#### Step (스텝)

시나리오 내 개별 실행 단위.

```typescript
type Step = {
  id: string;
  type: StepType;
  
  // 사람이 읽을 수 있는 설명
  description: string;           // "로그인 버튼 클릭"
  
  // 실행 설정
  timeout?: number;              // 개별 타임아웃
  continueOnError?: boolean;     // 실패해도 계속 진행
  
  // 타입별 설정
  config: StepConfig;
};

type StepType = 
  | 'navigate'      // 페이지 이동
  | 'click'         // 요소 클릭
  | 'fill'          // 입력 필드 채우기
  | 'select'        // 드롭다운 선택
  | 'hover'         // 마우스 호버
  | 'wait'          // 대기
  | 'assert'        // 검증
  | 'screenshot'    // 스크린샷 캡처
  | 'api-request'   // HTTP API 호출
  | 'api-assert'    // API 응답 검증
  | 'component'     // 재사용 컴포넌트 호출
  | 'script';       // 커스텀 스크립트

type StepConfig = 
  | NavigateConfig
  | ClickConfig
  | FillConfig
  | AssertConfig
  | ApiRequestConfig
  | ComponentConfig
  // ... 기타
```

#### ElementLocator (요소 로케이터)

Self-Healing의 핵심. 다층 셀렉터 전략.

```typescript
interface ElementLocator {
  // 사람이 읽을 수 있는 이름 (Healing 시 참조)
  displayName: string;           // "로그인 버튼"
  
  // 다층 전략 (우선순위 순)
  strategies: LocatorStrategy[];
  
  // Self-Healing 설정
  healing: {
    enabled: boolean;            // 기본 true
    autoApprove: boolean;        // 신뢰도 높으면 자동 승인
    confidenceThreshold: number; // 자동 승인 임계값 (0.0-1.0)
  };
}

type LocatorStrategy = 
  | { 
      type: 'testId'; 
      value: string;              // data-testid 값
      priority: number;           // 1 (최우선)
    }
  | { 
      type: 'role'; 
      role: string;               // ARIA role
      name?: string;              // accessible name
      priority: number;           // 2
    }
  | { 
      type: 'text'; 
      value: string;              // 텍스트 내용
      exact: boolean;             // 정확히 일치 여부
      priority: number;           // 3
    }
  | { 
      type: 'label'; 
      value: string;              // label 텍스트 (input용)
      priority: number;           // 4
    }
  | { 
      type: 'css'; 
      selector: string;           // CSS 선택자
      priority: number;           // 5 (최후 수단)
    }
  | {
      type: 'xpath';
      expression: string;
      priority: number;           // 6 (레거시/특수 케이스)
    };
```

#### Component (재사용 컴포넌트)

공통 플로우를 컴포넌트로 추출.

```typescript
interface Component {
  id: string;
  
  name: string;                  // "관리자 로그인"
  description?: string;
  
  // 타입
  type: 'flow' | 'assertion' | 'setup' | 'teardown';
  
  // 파라미터 정의
  parameters: ParameterDef[];
  
  // 스텝
  steps: Step[];
  
  createdAt: Date;
  updatedAt: Date;
}

interface ParameterDef {
  name: string;                  // "email"
  type: 'string' | 'number' | 'boolean' | 'enum';
  required: boolean;
  defaultValue?: any;
  options?: string[];            // enum일 때 선택지
  description?: string;
}
```

### 3.2 실행 및 결과

#### TestRun (실행)

```typescript
interface TestRun {
  id: string;
  scenarioId: string;
  
  status: 'pending' | 'running' | 'passed' | 'failed' | 'cancelled';
  
  startedAt?: Date;
  finishedAt?: Date;
  duration?: number;             // ms
  
  // 환경 정보
  environment: {
    baseUrl: string;
    variables: Record<string, any>;
  };
  
  // 결과 요약
  summary?: {
    totalSteps: number;
    passedSteps: number;
    failedSteps: number;
    skippedSteps: number;
    healedSteps: number;         // Self-Healing 발생 횟수
  };
  
  createdAt: Date;
}
```

#### StepResult (스텝 결과)

```typescript
interface StepResult {
  id: string;
  runId: string;
  stepId: string;
  stepIndex: number;
  
  status: 'passed' | 'failed' | 'skipped' | 'healed';
  
  duration: number;              // ms
  error?: {
    message: string;
    stack?: string;
  };
  
  // Self-Healing 정보
  healing?: {
    originalStrategy: LocatorStrategy;
    usedStrategy: LocatorStrategy;
    confidence: number;
  };
  
  // 디버깅용 컨텍스트
  context?: {
    screenshotPath?: string;
    htmlSnapshotPath?: string;
    consoleLog?: string[];
  };
  
  createdAt: Date;
}
```

### 3.3 Self-Healing

#### HealingRecord (치유 기록)

```typescript
interface HealingRecord {
  id: string;
  
  // 출처
  scenarioId: string;
  stepId: string;
  runId: string;
  
  // 변경 내용
  locatorDisplayName: string;    // "로그인 버튼"
  originalStrategy: LocatorStrategy;
  healedStrategy: LocatorStrategy;
  
  // 발생 원인
  trigger: 'element_not_found' | 'multiple_matches' | 'wrong_element';
  confidence: number;            // 0.0-1.0
  
  // 상태
  status: 'pending' | 'approved' | 'rejected' | 'auto_approved';
  reviewedBy?: string;
  reviewedAt?: Date;
  reviewNote?: string;
  
  // 전파 (같은 로케이터 쓰는 다른 시나리오에 적용)
  propagatedTo?: string[];       // 적용된 시나리오 ID들
  
  createdAt: Date;
}
```

#### ElementRegistry (요소 레지스트리)

동일 요소의 변경 이력 추적. Self-Healing 품질 향상용.

```typescript
interface ElementRegistry {
  id: string;
  serviceId: string;
  
  // 논리적 식별
  displayName: string;           // "지원자 삭제 버튼"
  pagePattern?: string;          // URL 패턴
  
  // 현재 로케이터
  currentLocator: ElementLocator;
  
  // 변경 이력
  history: {
    locator: ElementLocator;
    changedAt: Date;
    reason?: string;
  }[];
  
  // 사용처 추적
  usedIn: {
    scenarioId: string;
    stepId: string;
  }[];
  
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 4. API 설계

### 4.1 RESTful API 엔드포인트

#### Services

```
GET    /api/services                 # 서비스 목록
POST   /api/services                 # 서비스 생성
GET    /api/services/:id             # 서비스 상세
PUT    /api/services/:id             # 서비스 수정
DELETE /api/services/:id             # 서비스 삭제
```

#### Features

```
GET    /api/services/:serviceId/features      # 기능 목록
POST   /api/services/:serviceId/features      # 기능 생성
GET    /api/features/:id                      # 기능 상세
PUT    /api/features/:id                      # 기능 수정
DELETE /api/features/:id                      # 기능 삭제
```

#### Scenarios

```
GET    /api/features/:featureId/scenarios     # 시나리오 목록
POST   /api/features/:featureId/scenarios     # 시나리오 생성
GET    /api/scenarios/:id                     # 시나리오 상세
PUT    /api/scenarios/:id                     # 시나리오 수정
DELETE /api/scenarios/:id                     # 시나리오 삭제

# 시나리오 복제
POST   /api/scenarios/:id/duplicate
```

#### Components

```
GET    /api/components                        # 컴포넌트 목록
POST   /api/components                        # 컴포넌트 생성
GET    /api/components/:id                    # 컴포넌트 상세
PUT    /api/components/:id                    # 컴포넌트 수정
DELETE /api/components/:id                    # 컴포넌트 삭제

# 컴포넌트 사용처 조회
GET    /api/components/:id/usages
```

#### Test Execution

```
# 실행
POST   /api/scenarios/:id/run                 # 시나리오 실행
POST   /api/features/:id/run                  # 기능 전체 실행
POST   /api/services/:id/run                  # 서비스 전체 실행

# 실행 결과
GET    /api/runs                              # 실행 목록 (필터링)
GET    /api/runs/:id                          # 실행 상세
GET    /api/runs/:id/steps                    # 스텝 결과 목록
DELETE /api/runs/:id                          # 실행 취소

# 실시간 상태 (SSE)
GET    /api/runs/:id/stream                   # 실행 상태 스트림
```

#### Self-Healing

```
GET    /api/healing                           # 치유 기록 목록 (필터링)
GET    /api/healing/:id                       # 치유 기록 상세
POST   /api/healing/:id/approve               # 치유 승인
POST   /api/healing/:id/reject                # 치유 거부
POST   /api/healing/:id/propagate             # 다른 시나리오에 전파

# 대시보드용 통계
GET    /api/healing/stats                     # 치유 통계
```

### 4.2 실시간 통신

테스트 실행 상태를 실시간으로 전달하기 위해 Server-Sent Events (SSE) 사용.

```typescript
// GET /api/runs/:id/stream

// Event types
type RunEvent = 
  | { type: 'run:started'; data: { runId: string } }
  | { type: 'step:started'; data: { stepIndex: number; description: string } }
  | { type: 'step:passed'; data: { stepIndex: number; duration: number } }
  | { type: 'step:failed'; data: { stepIndex: number; error: string } }
  | { type: 'step:healed'; data: { stepIndex: number; healing: HealingInfo } }
  | { type: 'run:finished'; data: { status: string; summary: RunSummary } };
```

---

## 5. 핵심 기능 상세

### 5.1 테스트 실행 엔진

#### 실행 흐름

```
1. 시나리오 로드
2. 변수 초기화 (환경 변수 + 시나리오 변수)
3. 브라우저 컨텍스트 생성 (Playwright)
4. 스텝 순차 실행
   ├─ 컴포넌트 스텝 → 컴포넌트 스텝들로 확장
   ├─ 브라우저 스텝 → Playwright 실행
   │   └─ 요소 찾기 → 다층 셀렉터 해석 → Self-Healing
   └─ API 스텝 → HTTP 요청 + 검증
5. 결과 저장
6. 브라우저 정리
```

#### Locator Resolution (셀렉터 해석)

```typescript
class LocatorResolver {
  async resolve(
    locator: ElementLocator, 
    page: Page
  ): Promise<ResolveResult> {
    
    // 우선순위 순으로 전략 시도
    const sortedStrategies = [...locator.strategies]
      .sort((a, b) => a.priority - b.priority);
    
    for (const strategy of sortedStrategies) {
      const result = await this.tryStrategy(strategy, page);
      
      if (result.found) {
        // 첫 번째 전략이 아니면 Healing 발생
        const isHealed = strategy !== sortedStrategies[0];
        
        return {
          element: result.element,
          usedStrategy: strategy,
          healed: isHealed,
          confidence: result.confidence,
        };
      }
    }
    
    // 모든 전략 실패
    throw new ElementNotFoundError(locator);
  }
  
  private async tryStrategy(
    strategy: LocatorStrategy, 
    page: Page
  ): Promise<StrategyResult> {
    switch (strategy.type) {
      case 'testId':
        return this.findByTestId(page, strategy.value);
      case 'role':
        return this.findByRole(page, strategy.role, strategy.name);
      case 'text':
        return this.findByText(page, strategy.value, strategy.exact);
      case 'css':
        return this.findByCss(page, strategy.selector);
      // ...
    }
  }
}
```

### 5.2 Self-Healing 시스템

#### Healing 발생 조건

1. **element_not_found**: 첫 번째 전략 실패, 다른 전략으로 찾음
2. **multiple_matches**: 여러 요소 매칭, 가장 적합한 것 선택
3. **wrong_element**: 요소를 찾았으나 예상과 다름 (위치, 속성 등)

#### Healing 처리 흐름

```
1. 원래 전략으로 요소 찾기 시도
2. 실패 시 → 다른 전략들 순차 시도
3. 찾으면 → HealingRecord 생성
   ├─ confidence >= threshold && autoApprove → auto_approved
   └─ else → pending (승인 대기)
4. 다음 실행 시
   ├─ approved/auto_approved → healed 전략을 첫 번째로 사용
   └─ pending → 원래 전략 유지 (매번 healing 발생)
5. 전파 (선택)
   └─ 같은 ElementRegistry의 요소를 쓰는 다른 시나리오에 적용
```

#### Confidence 계산

```typescript
function calculateConfidence(
  original: LocatorStrategy,
  healed: LocatorStrategy,
  context: HealingContext
): number {
  let score = 1.0;
  
  // 전략 타입 변경 패널티
  const typePenalty: Record<string, number> = {
    'testId→role': 0.1,
    'testId→text': 0.15,
    'testId→css': 0.3,
    'role→text': 0.1,
    'role→css': 0.25,
    'text→css': 0.2,
  };
  const key = `${original.type}→${healed.type}`;
  score -= typePenalty[key] ?? 0.2;
  
  // 위치 변경 패널티
  if (context.positionChanged) {
    score -= 0.1;
  }
  
  // 부모 요소 변경 패널티
  if (context.parentChanged) {
    score -= 0.15;
  }
  
  // 텍스트 유사도 보너스
  if (context.textSimilarity > 0.9) {
    score += 0.1;
  }
  
  return Math.max(0, Math.min(1, score));
}
```

### 5.3 컴포넌트 시스템

#### 컴포넌트 호출

```typescript
// 시나리오 스텝에서 컴포넌트 참조
const step: Step = {
  id: 'step-1',
  type: 'component',
  description: '관리자로 로그인',
  config: {
    componentId: 'comp-login-admin',
    parameters: {
      email: '{{adminEmail}}',    // 변수 참조
      password: '{{adminPassword}}'
    }
  }
};

// 실행 시 컴포넌트 스텝으로 확장
function expandComponent(step: ComponentStep, component: Component): Step[] {
  return component.steps.map(compStep => ({
    ...compStep,
    // 파라미터 바인딩
    config: bindParameters(compStep.config, step.config.parameters)
  }));
}
```

#### 컴포넌트 사용처 추적

컴포넌트 변경 시 영향받는 시나리오 파악.

```typescript
// GET /api/components/:id/usages
interface ComponentUsage {
  component: Component;
  usedBy: {
    scenario: Scenario;
    stepIndices: number[];  // 해당 시나리오에서 몇 번째 스텝들이 사용하는지
  }[];
  totalUsages: number;
}
```

---

## 6. UI 설계

### 6.1 페이지 구조

```
/                           # 대시보드 (최근 실행, 통계)
/services                   # 서비스 목록
/services/:id               # 서비스 상세 (기능 목록)
/features/:id               # 기능 상세 (시나리오 목록)
/scenarios/:id              # 시나리오 상세 & 에디터
/scenarios/:id/runs/:runId  # 실행 결과 상세
/components                 # 컴포넌트 목록
/components/:id             # 컴포넌트 에디터
/healing                    # Self-Healing 대시보드
/runs                       # 실행 이력
```

### 6.2 핵심 화면

#### 6.2.1 대시보드

```
┌─────────────────────────────────────────────────────────────────┐
│  TestForge                                    [+ 새 서비스]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─ 최근 24시간 ──────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │  전체 실행: 47    성공: 42    실패: 3    치유됨: 2         │ │
│  │                                                           │ │
│  │  [============================----]  89.4% 성공률         │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ Self-Healing 현황 ───────────────────────────────────────┐ │
│  │                                                           │ │
│  │  ⚠️ 승인 대기 3건                      [검토하기 →]       │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 최근 실패 ───────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │  ❌ 지원자 삭제 테스트          10분 전     [상세 →]       │ │
│  │  ❌ 결제 취소 플로우            2시간 전    [상세 →]       │ │
│  │  ❌ 로그인 실패 케이스          3시간 전    [상세 →]       │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 6.2.2 시나리오 에디터

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 지원자 관리                          [저장] [▶️ 실행]        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  시나리오: 지원자 상태 변경 - 서류합격                           │
│                                                                 │
│  ┌─ 기본 정보 ───────────────────────────────────────────────┐ │
│  │  우선순위: [높음 ▼]                                       │ │
│  │  태그: [회귀] [스모크] [+ 추가]                            │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 변수 ────────────────────────────────────────────────────┐ │
│  │  applicantName   string   "홍길동"                        │ │
│  │  [+ 변수 추가]                                            │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 스텝 ────────────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │  1. 🔄 관리자 로그인 (컴포넌트)                ≡ [⋮]     │ │
│  │     └─ email: admin@test.com                             │ │
│  │                                                           │ │
│  │  2. 🌐 페이지 이동                              ≡ [⋮]     │ │
│  │     └─ /admin/applicants                                 │ │
│  │                                                           │ │
│  │  3. 👆 클릭                                     ≡ [⋮]     │ │
│  │     └─ "{{applicantName}}" 행의 "상태 변경" 버튼          │ │
│  │     └─ 로케이터: testId > role > text                    │ │
│  │                                                           │ │
│  │  4. 👆 클릭                                     ≡ [⋮]     │ │
│  │     └─ "서류합격" 옵션                                    │ │
│  │                                                           │ │
│  │  5. ✓ 검증                                     ≡ [⋮]     │ │
│  │     └─ 토스트 메시지 "상태가 변경되었습니다" 표시          │ │
│  │                                                           │ │
│  │  [+ 스텝 추가]                                            │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 6.2.3 스텝 편집 모달

```
┌─────────────────────────────────────────────────────────────────┐
│  스텝 편집: 클릭                                        [✕]    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  설명                                                           │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ "상태 변경" 버튼 클릭                                      │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  요소 선택 방법                                                 │
│                                                                 │
│  ┌─ 전략 1 (우선) ───────────────────────────────────────────┐ │
│  │  타입: [data-testid ▼]                                    │ │
│  │  값:   status-change-btn                                  │ │
│  │                                                  [🗑️ 삭제] │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 전략 2 (폴백) ───────────────────────────────────────────┐ │
│  │  타입: [역할(Role) ▼]                                     │ │
│  │  Role: button                                             │ │
│  │  Name: 상태 변경                                          │ │
│  │                                                  [🗑️ 삭제] │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 전략 3 (폴백) ───────────────────────────────────────────┐ │
│  │  타입: [텍스트 ▼]                                         │ │
│  │  값:   상태 변경                                          │ │
│  │  ☑️ 정확히 일치                                           │ │
│  │                                                  [🗑️ 삭제] │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  [+ 전략 추가]                                                  │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  Self-Healing 설정                                              │
│  ☑️ Self-Healing 활성화                                         │
│  ☐ 높은 신뢰도 시 자동 승인 (임계값: 0.9)                       │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  고급 설정                                                      │
│  타임아웃: [30000] ms                                           │
│  ☐ 실패해도 계속 진행                                           │
│                                                                 │
│                                          [취소] [저장]          │
└─────────────────────────────────────────────────────────────────┘
```

#### 6.2.4 Self-Healing 대시보드

```
┌─────────────────────────────────────────────────────────────────┐
│  Self-Healing                                      최근 7일 ▼  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  자동 승인   │  │  승인 대기   │  │  실패       │              │
│  │     23      │  │      5      │  │      2      │              │
│  │  ✓ 정상     │  │  ⚠️ 검토필요 │  │  ❌ 복구실패 │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  필터: [전체 상태 ▼] [전체 서비스 ▼]              🔍 검색       │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  ┌─ 승인 대기 ───────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │  📍 "상태 변경" 버튼                          2시간 전    │ │
│  │  ├─ 시나리오: 지원자 상태 변경 - 서류합격                 │ │
│  │  ├─ 원인: element_not_found                              │ │
│  │  ├─ 변경: testId → role (button, "상태 변경")            │ │
│  │  ├─ 신뢰도: 94%                                          │ │
│  │  ├─ 영향: 4개 시나리오                                   │ │
│  │  │                                                       │ │
│  │  │        [✅ 승인] [✅ 전체 승인] [❌ 거부] [상세 →]    │ │
│  │  │                                                       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 승인 대기 ───────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │  📍 API 응답 필드 변경                        5시간 전    │ │
│  │  ├─ 시나리오: 지원자 목록 API 테스트                      │ │
│  │  ├─ 변경: data.status → data.state                       │ │
│  │  ├─ 신뢰도: 87%                                          │ │
│  │  │                                                       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 6.2.5 실행 결과 상세

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 지원자 상태 변경 - 서류합격                     [🔄 재실행]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  실행 #run-abc123                                               │
│  상태: ✅ 성공 (치유 1건)                                        │
│  시간: 2026-02-07 14:23:15  (12.4초 소요)                       │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  ┌─ 스텝 결과 ───────────────────────────────────────────────┐ │
│  │                                                           │ │
│  │  ✅ 1. 관리자 로그인                           2.1s       │ │
│  │  ✅ 2. 페이지 이동 /admin/applicants           1.5s       │ │
│  │  ⚠️ 3. "상태 변경" 버튼 클릭                   3.2s 치유됨 │ │
│  │     └─ testId 실패 → role로 찾음 (신뢰도 94%)             │ │
│  │     └─ [Healing 승인 →]                                   │ │
│  │  ✅ 4. "서류합격" 옵션 클릭                     0.8s       │ │
│  │  ✅ 5. 토스트 메시지 검증                      0.3s       │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 스텝 3 상세 (클릭하여 확장됨) ───────────────────────────┐ │
│  │                                                           │ │
│  │  스크린샷:                                                │ │
│  │  ┌─────────────────────────────────────┐                  │ │
│  │  │                                     │                  │ │
│  │  │        [스크린샷 이미지]            │                  │ │
│  │  │                                     │                  │ │
│  │  └─────────────────────────────────────┘                  │ │
│  │                                                           │ │
│  │  시도한 전략:                                             │ │
│  │  ❌ testId="status-change-btn" - 요소 없음                │ │
│  │  ✅ role=button, name="상태 변경" - 찾음                  │ │
│  │                                                           │ │
│  │  콘솔 로그:                                               │ │
│  │  [info] Navigated to /admin/applicants                   │ │
│  │  [warn] Element not found with testId                    │ │
│  │  [info] Healed: using role strategy                      │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 개발 로드맵

### Phase 1: 기반 구축 (2주)

**목표**: 프로젝트 구조 설정, 기본 CRUD, 단순 테스트 실행

**Week 1**
- [ ] Bun workspace 프로젝트 초기화
- [ ] Drizzle ORM + SQLite 설정
- [ ] Service, Feature, Scenario CRUD API
- [ ] React 앱 기본 구조 (라우팅, 레이아웃)
- [ ] 서비스/기능/시나리오 목록 UI

**Week 2**
- [ ] Scenario 에디터 UI (스텝 추가/수정/삭제)
- [ ] 기본 스텝 타입 구현 (navigate, click, fill, assert)
- [ ] Playwright 연동 - 단순 테스트 실행
- [ ] 실행 결과 저장 및 조회

**산출물**
- 시나리오 생성 → 실행 → 결과 확인 가능
- 단일 CSS 셀렉터로 요소 찾기

---

### Phase 2: 다층 셀렉터 & Self-Healing (2주)

**목표**: 다층 로케이터 시스템, Self-Healing 기본 동작

**Week 3**
- [ ] ElementLocator 모델 구현
- [ ] 다층 셀렉터 해석기 (testId → role → text → css)
- [ ] 스텝 에디터에 로케이터 UI 추가
- [ ] 실행 시 셀렉터 우선순위 적용

**Week 4**
- [ ] HealingRecord 모델 및 API
- [ ] Healing 감지 및 기록
- [ ] Healing 대시보드 UI
- [ ] Healing 승인/거부 워크플로우
- [ ] 자동 승인 (confidence threshold)

**산출물**
- 다층 셀렉터로 요소 찾기
- UI 변경 시 자동으로 다른 전략 시도
- Healing 발생 시 기록 및 승인 가능

---

### Phase 3: 컴포넌트 & API 테스트 (2주)

**목표**: 재사용 컴포넌트, API 테스트 지원

**Week 5**
- [ ] Component 모델 및 CRUD API
- [ ] 컴포넌트 에디터 UI
- [ ] 시나리오에서 컴포넌트 참조
- [ ] 컴포넌트 파라미터 바인딩
- [ ] 컴포넌트 사용처 추적

**Week 6**
- [ ] API 테스트 스텝 타입 (api-request, api-assert)
- [ ] HTTP 요청 실행 및 검증
- [ ] API 응답 스키마 검증 (Zod)
- [ ] API 필드 변경 감지 (Self-Healing 확장)

**산출물**
- 공통 플로우를 컴포넌트로 추출
- API 엔드포인트 테스트 가능
- API 응답 변경 시 Healing 시도

---

### Phase 4: 안정화 & UX 개선 (1주)

**목표**: 에러 처리, 성능, 사용성 개선

**Week 7**
- [ ] 에러 처리 및 사용자 피드백 개선
- [ ] 실행 중 실시간 상태 표시 (SSE)
- [ ] 스크린샷 캡처 및 표시
- [ ] 시나리오 복제 기능
- [ ] 검색 및 필터링
- [ ] 문서화 (README, 사용 가이드)

**산출물**
- 안정적인 MVP
- 사용 가이드 문서

---

## 8. 기술적 고려사항

### 8.1 Bun 특이사항

```typescript
// Bun 네이티브 기능 활용
// 파일 I/O, 프로세스 관리 등
```

### 8.2 DuckDB 설정

```typescript
// DuckDB - 분석 친화적 임베디드 DB
import { Database } from "duckdb-async";

const db = await Database.create("testforge.duckdb");

// Drizzle ORM과 연동 (커스텀 드라이버 필요)
// 또는 직접 SQL 사용
await db.run(`
  CREATE TABLE IF NOT EXISTS services (
    id VARCHAR PRIMARY KEY,
    name VARCHAR NOT NULL,
    base_url VARCHAR,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
  )
`);

// DuckDB 장점:
// - 복잡한 분석 쿼리 (실행 통계, 트렌드 등)
// - 컬럼 기반 저장으로 집계 빠름
// - 단일 파일, 서버리스
// - Parquet 등 다양한 포맷 지원
```

### 8.3 Hono RPC (타입 안전 통신)

```typescript
// packages/server/src/routes/scenarios.ts
import { Hono } from "hono";
import { zValidator } from "@hono/zod-validator";

const app = new Hono()
  .get("/", async (c) => {
    const scenarios = await getScenarios();
    return c.json(scenarios);
  })
  .post(
    "/",
    zValidator("json", createScenarioSchema),
    async (c) => {
      const data = c.req.valid("json");
      const scenario = await createScenario(data);
      return c.json(scenario, 201);
    }
  )
  .get("/:id", async (c) => {
    const id = c.req.param("id");
    const scenario = await getScenario(id);
    return c.json(scenario);
  })
  .post("/:id/run", async (c) => {
    const id = c.req.param("id");
    const run = await executeScenario(id);
    return c.json(run);
  });

export type ScenariosRoute = typeof app;
export default app;

// packages/server/src/index.ts
import scenarios from "./routes/scenarios";
import services from "./routes/services";

const app = new Hono()
  .route("/api/scenarios", scenarios)
  .route("/api/services", services);

export type AppType = typeof app;
export default app;
```

```typescript
// packages/web/src/lib/rpc.ts
import { hc } from "hono/client";
import type { AppType } from "@testforge/server";
import axios from "axios";

// Hono RPC 클라이언트 (타입 추론)
export const api = hc<AppType>("http://localhost:3001");

// 사용 예시 (완전한 타입 안전)
// const scenarios = await api.api.scenarios.$get();
// const scenario = await api.api.scenarios[":id"].$get({ param: { id: "123" } });
// const run = await api.api.scenarios[":id"].run.$post({ param: { id: "123" } });

// Axios 래퍼 (필요한 경우 인터셉터 등 활용)
export const axiosClient = axios.create({
  baseURL: "http://localhost:3001",
  headers: {
    "Content-Type": "application/json",
  },
});

// 인터셉터 예시
axiosClient.interceptors.response.use(
  (response) => response,
  (error) => {
    // 에러 핸들링
    console.error("API Error:", error.response?.data);
    return Promise.reject(error);
  }
);
```

```typescript
// packages/web/src/pages/scenarios/index.tsx
import { api } from "@/lib/rpc";
import { useQuery, useMutation } from "@tanstack/react-query";

export function ScenariosPage() {
  // RPC 클라이언트로 타입 안전한 API 호출
  const { data: scenarios } = useQuery({
    queryKey: ["scenarios"],
    queryFn: async () => {
      const res = await api.api.scenarios.$get();
      return res.json();
    },
  });

  const runMutation = useMutation({
    mutationFn: async (id: string) => {
      const res = await api.api.scenarios[":id"].run.$post({
        param: { id },
      });
      return res.json();
    },
  });

  // ...
}
```

### 8.4 Playwright 관리

```typescript
// 테스트 실행 시 브라우저 컨텍스트 격리
const browser = await chromium.launch({ headless: true });
const context = await browser.newContext({
  viewport: { width: 1280, height: 720 },
  // 녹화 (디버깅용)
  recordVideo: { dir: './videos' }
});
const page = await context.newPage();

// 실행 후 정리
await context.close();
await browser.close();
```

### 8.5 에러 처리

```typescript
// 커스텀 에러 타입
class TestForgeError extends Error {
  constructor(
    message: string,
    public code: string,
    public details?: Record<string, any>
  ) {
    super(message);
  }
}

class ElementNotFoundError extends TestForgeError {
  constructor(locator: ElementLocator) {
    super(
      `Element not found: ${locator.displayName}`,
      'ELEMENT_NOT_FOUND',
      { locator }
    );
  }
}

class HealingFailedError extends TestForgeError {
  constructor(locator: ElementLocator, attemptedStrategies: LocatorStrategy[]) {
    super(
      `All healing strategies failed for: ${locator.displayName}`,
      'HEALING_FAILED',
      { locator, attemptedStrategies }
    );
  }
}
```

### 8.6 동시 실행 제한

MVP에서는 단일 실행만 지원. 동시 실행 시 충돌 방지.

```typescript
// 간단한 실행 락
class ExecutionLock {
  private running = false;
  
  async acquire(): Promise<boolean> {
    if (this.running) return false;
    this.running = true;
    return true;
  }
  
  release() {
    this.running = false;
  }
}
```

---

## 9. 성공 기준

### MVP 완료 기준

1. **기본 흐름 동작**
   - [ ] 서비스 → 기능 → 시나리오 계층 구조로 테스트 관리
   - [ ] 시나리오 생성, 수정, 삭제
   - [ ] 브라우저 테스트 실행 및 결과 확인

2. **Self-Healing 동작**
   - [ ] 다층 셀렉터 (testId, role, text, css) 지원
   - [ ] 첫 번째 전략 실패 시 자동으로 다음 전략 시도
   - [ ] Healing 발생 시 기록 생성
   - [ ] Healing 승인/거부 가능
   - [ ] 승인된 Healing이 다음 실행에 반영

3. **컴포넌트 재사용**
   - [ ] 공통 플로우를 컴포넌트로 추출
   - [ ] 시나리오에서 컴포넌트 참조
   - [ ] 컴포넌트 변경 시 사용처 파악

4. **API 테스트**
   - [ ] HTTP 요청 실행
   - [ ] 응답 상태 코드 및 필드 검증

---

## 10. 용어 정의

| 용어 | 정의 |
|------|------|
| **Service** | 테스트 대상 애플리케이션 (예: 채용 서비스) |
| **Feature** | 서비스 내 논리적 기능 단위 (예: 지원자 관리) |
| **Scenario** | 실제 테스트 케이스 (예: 지원자 상태 변경) |
| **Step** | 시나리오 내 개별 실행 단위 (예: 버튼 클릭) |
| **Component** | 재사용 가능한 스텝 묶음 (예: 로그인 플로우) |
| **Locator** | 페이지 내 요소를 찾는 방법 |
| **Strategy** | 로케이터의 개별 찾기 전략 (testId, role 등) |
| **Self-Healing** | 요소를 찾지 못할 때 다른 전략으로 자동 복구 |
| **Healing Record** | Self-Healing 발생 기록 |
| **Confidence** | Healing 결과의 신뢰도 (0.0-1.0) |

---

## 부록 A: 스텝 타입 상세

### navigate

```typescript
interface NavigateConfig {
  url: string;                   // 절대 또는 상대 URL
  waitUntil?: 'load' | 'domcontentloaded' | 'networkidle';
}
```

### click

```typescript
interface ClickConfig {
  locator: ElementLocator;
  button?: 'left' | 'right' | 'middle';
  clickCount?: number;           // 더블클릭 등
  modifiers?: ('Alt' | 'Control' | 'Meta' | 'Shift')[];
}
```

### fill

```typescript
interface FillConfig {
  locator: ElementLocator;
  value: string;                 // 변수 참조 가능: "{{email}}"
  clear?: boolean;               // 기존 값 지우고 입력
}
```

### select

```typescript
interface SelectConfig {
  locator: ElementLocator;
  value?: string;                // option value
  label?: string;                // option 텍스트
  index?: number;                // option 인덱스
}
```

### assert

```typescript
interface AssertConfig {
  type: 'visible' | 'hidden' | 'text' | 'value' | 'url' | 'title' | 'count';
  
  // visible, hidden, text, value, count
  locator?: ElementLocator;
  
  // text, value
  expected?: string;
  matcher?: 'equals' | 'contains' | 'matches';  // matches는 정규식
  
  // url, title
  pattern?: string;
  
  // count
  count?: number;
  comparison?: 'eq' | 'gt' | 'gte' | 'lt' | 'lte';
}
```

### wait

```typescript
interface WaitConfig {
  type: 'time' | 'element' | 'url' | 'function';
  
  // time
  duration?: number;             // ms
  
  // element
  locator?: ElementLocator;
  state?: 'visible' | 'hidden' | 'attached' | 'detached';
  
  // url
  pattern?: string;
  
  // function (고급)
  expression?: string;           // 페이지에서 실행할 JS
}
```

### api-request

```typescript
interface ApiRequestConfig {
  method: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';
  url: string;
  headers?: Record<string, string>;
  body?: any;
  
  // 응답 저장 (다른 스텝에서 참조)
  saveAs?: string;               // "{{response}}"로 참조
}
```

### api-assert

```typescript
interface ApiAssertConfig {
  // 이전 api-request의 saveAs 참조
  response: string;              // "{{response}}"
  
  assertions: ApiAssertion[];
}

interface ApiAssertion {
  type: 'status' | 'header' | 'body';
  
  // status
  status?: number;
  
  // header
  headerName?: string;
  headerValue?: string;
  
  // body (JSON path)
  path?: string;                 // "data.user.name"
  expected?: any;
  matcher?: 'equals' | 'contains' | 'exists' | 'type';
}
```

### component

```typescript
interface ComponentConfig {
  componentId: string;
  parameters: Record<string, any>;
}
```

### screenshot

```typescript
interface ScreenshotConfig {
  name: string;                  // 파일명
  fullPage?: boolean;
  locator?: ElementLocator;      // 특정 요소만 캡처
}
```

---

## 부록 B: 변수 시스템

### 변수 우선순위

1. **스텝 로컬 변수** (api-request의 saveAs 등)
2. **시나리오 변수** (Scenario.variables)
3. **컴포넌트 파라미터** (Component 호출 시)
4. **서비스 기본 변수** (Service.defaultVariables)
5. **환경 변수** (실행 시 주입)

### 변수 참조 문법

```
{{variableName}}                 # 단순 참조
{{response.data.id}}             # 중첩 객체 접근
{{items[0].name}}                # 배열 접근
```

### 내장 변수

```
{{$timestamp}}                   # 현재 타임스탬프
{{$randomString}}                # 랜덤 문자열
{{$randomNumber}}                # 랜덤 숫자
{{$uuid}}                        # UUID v4
```

---

## 부록 C: 개발 환경 설정

### 필수 설치

```bash
# Bun 설치
curl -fsSL https://bun.sh/install | bash

# Playwright 브라우저 설치
bunx playwright install chromium

# DuckDB CLI (선택, 디버깅용)
# macOS
brew install duckdb
# 또는 https://duckdb.org/docs/installation 참고
```

### 프로젝트 시작

```bash
# 의존성 설치
bun install

# 개발 서버 시작 (API + Web 동시)
bun run dev

# DB 마이그레이션
bun run db:migrate

# 시드 데이터
bun run db:seed
```

### 스크립트 정의 (package.json)

```json
{
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "dev:server": "bun run --watch packages/server/src/index.ts",
    "dev:web": "bun run --cwd packages/web rsbuild dev",
    "build": "bun run build:server && bun run build:web",
    "build:server": "bun build packages/server/src/index.ts --outdir dist/server",
    "build:web": "bun run --cwd packages/web rsbuild build",
    "db:migrate": "bun run packages/server/src/db/migrate.ts",
    "db:seed": "bun run scripts/seed.ts",
    "test": "bun test"
  }
}
```

### shadcn/ui 설정

```bash
# packages/web에서 실행
bunx shadcn@latest init

# 컴포넌트 추가
bunx shadcn@latest add button card dialog table tabs toast
bunx shadcn@latest add form input label select textarea
bunx shadcn@latest add dropdown-menu context-menu
bunx shadcn@latest add badge separator skeleton
```

```typescript
// packages/web/src/components/ui/ 에 컴포넌트 생성됨
// 예: button.tsx, card.tsx, dialog.tsx 등

// 사용 예시
import { Button } from "@/components/ui/button";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { Dialog, DialogTrigger, DialogContent } from "@/components/ui/dialog";
```
